What It Does

An interactive CLI that lets you have a multi-turn conversation with Claude about your Java codebase. You point it at source files, ask Claude to generate or modify code, and it returns structured JSON with file contents you can write to disk.
Prerequisites

    Java 17+ (uses records, text blocks, switch expressions)
    Maven
    ANTHROPIC_API_KEY environment variable set
    An Anthropic account with API access
